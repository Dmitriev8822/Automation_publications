# Модули scheduler и main

`app/scheduler.py` создаёт APScheduler job с защитой от падения планировщика при ошибке публикации.

`app/main.py` является точкой входа: загружает настройки, валидирует их, запускает быстрые тесты, инициализирует БД, собирает зависимости и стартует планировщик.
