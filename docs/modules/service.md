# Модуль service

`app/service.py` содержит главный сценарий `create_and_publish_post()`.

Сервис получает AI-клиент, Telegram-публикатор и репозиторий снаружи, выбирает первую неопубликованную новость, генерирует пост, публикует его и сохраняет результат.
