[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not integration"
markers =
    unit: fast tests that do not use network, real external APIs, or production databases
    integration: slower tests that may require explicitly configured external services and are excluded from default startup checks
